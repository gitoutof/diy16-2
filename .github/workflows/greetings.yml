name: Greetings

on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'Person to greet'
        default: 'World'
        required: true


jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout
      uses: actions/checkout@main

